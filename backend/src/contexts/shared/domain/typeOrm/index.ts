export enum RemovedType {
    REMOVED = '0',
    NOT_REMOVED = '1'
}

export enum StatusType {
    INACTIVE = '0',
    ACTIVE = '1'
}

export enum ChildrenType {
    NOT_HAS = '0',
    HAS = '1'
}

export enum LockedType {
    LOCKED = '0',
    NOT_LOCKED = '1'
}
